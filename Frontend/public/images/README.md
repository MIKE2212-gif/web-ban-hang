Place the decorative image for the auth page here.

- Filename to use in the app: `auth-image.svg` (recommended) or `auth-image.png`.
- Recommended size: at least 1200×800px (or similar aspect ratio) to ensure good coverage on large screens.
- To use your attached photo: copy it into this folder and replace `auth-image.svg` (or update `Auth.jsx` to point to the filename you choose).
